"""
BizFlow — веб-приложение для организации обслуживания клиентов малого бизнеса.

Формат проекта по вашему требованию: ВЕСЬ проект — один файл `BizFlow` (этот файл).

## Установка и запуск (Windows / PowerShell)
1) Установите Python 3.10+
2) В папке `d:\\BizFlow` выполните:

   pip install flask werkzeug
   python .\\BizFlow

3) Откройте в браузере: http://127.0.0.1:5000

## 3 ручных теста (основные сценарии)
1) Регистрация и вход:
   - Страница "Регистрация" → создайте пользователя → затем "Войти".
   - Ожидаемо: попадёте в "Дашборд", в шапке видно email.

2) Проверка некорректного ввода:
   - "Клиенты" → "Добавить клиента" → оставьте "ФИО" пустым → "Сохранить".
   - Ожидаемо: сообщение об ошибке, приложение не падает.

3) Сохранение между запусками (SQLite):
   - Добавьте клиента, перезапустите сервер (Ctrl+C, затем снова `python .\\BizFlow`).
   - Ожидаемо: клиент остался в списке.

## GitHub/GitFlic (критерий публикации)
Создайте публичный репозиторий и добавьте этот файл. В README можно скопировать раздел "Установка и запуск" выше.
"""
